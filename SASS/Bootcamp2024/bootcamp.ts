const secret: string = "1234";

let attempts: string[] = ["1111", "2222", "3333", "1234"];


function checkPassword:string(let :string attempt[]) 
{
   let i :number = 0; 
    do {
        const  correctCode:string;

        switch (attempt) {
            case attempt[i]:
                
            return "✅ Access granted (2 attempts)";
        
            case attempt[i]
            default:
                break;
                console.l
        }
        
    } while ();

    return 
}